<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- Expone los tipos internal (Lexer, Parser, Token, Interpreter, ...) al proyecto de pruebas. -->
  <ItemGroup>
    <InternalsVisibleTo Include="OwnLang.Tests" />
  </ItemGroup>
  <ItemGroup>
    <EmbeddedResource Include="prelude.own" />
  </ItemGroup>

</Project>
